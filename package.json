{
  "name": "apollo-mock-operations",
  "private": "true",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Frank Cooke",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build --stream",
    "build:watch": "npx lerna run build:watch --stream",
    "clean": "npx lerna run clean --stream",
    "lint": "npx eslint \"**/*.{ts,tsx,js}\"",
    "publish:ci": "yarn build && npx lerna publish -y",
    "pretest": "yarn build",
    "test": "npx jest --maxWorkers=50%",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
