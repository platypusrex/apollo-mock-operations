import { OperationStateConfig } from '@apollo-mock-operations/codegen-plugin';

const config: OperationStateConfig = {
  operationState: {
    /* add your operation state here */
  },
};

export default config;
